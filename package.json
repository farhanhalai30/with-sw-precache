{
  "name": "with-sw-precache",
  "version": "1.0.0",
  "scripts": {
    "build": "next build",
    "local-start": "set NODE_ENV=production&node server.js",
    "start": "NODE_ENV=production node server.js",
    "export": "next export",
    "deploy": "npm run build && npm run export"
  },
  "dependencies": {
    "next": "^8.0.4",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "sw-precache-webpack-plugin": "^0.11.3"
  }
}
